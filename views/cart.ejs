<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Golden Drip Cafe</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Arial', sans-serif;
        }
        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
            margin-bottom: 1.5rem;
        }
        .card-title {
            color: #343a40;
            font-weight: bold;
        }
        .card-text {
            color: #6c757d;
        }
        .btn {
            border-radius: 20px;
        }
        .menu-section {
            margin: 2rem auto;
            max-width: 800px;
        }
        .menu-header {
            text-align: center;
            margin-bottom: 2rem;
            font-size: 2rem;
            color: #212529;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="container menu-section">
        <h1 class="menu-header">Welcome to Golden Drip Cafe</h1>

        <!-- Chicken Dum Biryani Card -->
         <% items.forEach(menu => { %>
            <div class="card">
                <div class="row g-0">
                    <div class="col-md-4">
                        <img src=<%= menu.image %> 
                             class="img-fluid" alt=<%= menu.name %>>
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title"><%= menu.name %></h5>
                            <p class="card-text"><%= menu.description %></p>
                            <div>
                                <div class="btn-group dropend">
                                    <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                      Qty: 
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li><a class="dropdown-item" value = 1 >1</a></li>
                                        <li><a class="dropdown-item" value = 2 >2</a></li>
                                        <li><a class="dropdown-item" value = 3>3</a></li>
                                        <li><a class="dropdown-item" value = 4>4</a></li>
                                        <li><a class="dropdown-item" value = 5>5</a></li>
                                    </ul>
                                  </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
         <% }) %>
        

        <!-- Chicken Chap Card
        <div class="card">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="https://atanurrannagharrecipe.com/wp-content/uploads/2022/12/Chicken-Chap-photo-01.jpg" 
                         class="img-fluid" alt="Chicken Chap">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">Chicken Chap</h5>
                        <p class="card-text">2 pcs chicken chap</p>
                        <div>
                          <button class="btn btn-danger btn-sm" onclick="updateCount('chap', -1)">-</button>
                          <span id="chap" class="px-3">0</span>
                          <button class="btn btn-primary btn-sm" onclick="updateCount('chap', 1)">+</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div> -->

    <%- include('./partials/scripts') %>

    <script>
         function updateCount(productId, increment) {
            const countElement = document.getElementById(productId);
            let count = parseInt(countElement.innerText);
            count += increment;
            if (count < 1) count = 1;
            if(count >20) count = 20 ; 
             
            countElement.innerText = count;
        }
    </script>
</body>
</html>
